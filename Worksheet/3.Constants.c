#include <stdio.h>

int main()
    {
        //Constant or const = fixed value that can not be altered b the program during its execution

        const double PI = 3.14159;
        printf("\n%lf\n", PI); 

        return 0;
    }